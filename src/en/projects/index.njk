---
locale: "en"
dir: "ltr"
---

{% extends "layouts/base.njk" %}
{% block title %}Projects{% endblock %}
{% block content %}
  <div class="w-full max-w-6xl px-4 mx-auto mt-12 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 gap-6 lg:grid-cols-3">
      <div class="col-span-1">
        <h1 class="text-4xl font-bold leading-none">
          Projects
        </h1>
        <p class="mt-4 text-base font-medium text-gray-600">
          Here's some of the latest projects I've been
          working on either for OSS or freelance client work.
        </p>
      </div>
      <div class="grid grid-cols-1 gap-6 md:grid-cols-2 md:col-span-2">
        {%- for project in collections.englishProjects | reverse -%}
          <div class="flex flex-col rounded-md">
            <a href="{{project.data.external_url}}" class="border rounded-md ">
              <img src="{{project.data.image}}" alt="{{project.data.image_alt}}" title="{{project.data.image_alt}}" class="object-cover w-full bg-lime-300 rounded-md shadow-inner h-72">
            </a>
            <div class="mt-4">
              <h2 class="text-lg font-bold leading-5 text-gray-800">
                <a href="{{project.data.external_url}}" class="hover:text-gray-900 hover:underline focus:text-gray-900 focus:underline">{{project.data.title}}</a>
              </h2>
              <p class="mt-1 text-sm font-bold text-lime-800">
                {{ project.data.tags | join(", ") }}
              </p>
              <div class="mt-5 text-base leading-6 text-gray-700">
                {{ project.templateContent | safe }}
              </div>
            </div>
          </div>
        {%- endfor -%}
      </div>
    </div>
  </div>
{% endblock %}